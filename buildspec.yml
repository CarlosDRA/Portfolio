version: 0.2

phases:
  pre_build:
    commands:
      - echo looking at files
      - pwd
      - ls
  build:
    commands:
      - echo copying public build to S3
  post_build:
    commands:
      - echo Finished deploy