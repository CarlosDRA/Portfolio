version: 0.2

phases:
  pre_build:
    commands:
      - echo Started pre_build phase...
      - aws s3 cp --recursive . s3://www.carlosregaladodev.com
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      -echo Build complete